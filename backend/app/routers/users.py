"""
DEPRECATED - no longer used.

The product has no account/login system (team decision: removed for
privacy, per tutor's guidance), so there is no per-user data (preferences)
to scope. This module is not imported by app/main.py anymore. Delete this
file from the repo with:

    git rm backend/app/routers/users.py
"""
