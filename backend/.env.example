# --- Database (shared MySQL / AWS RDS) ---
# Same instance and same env var names as the repo-root db.py, so both
# files stay pointed at one database. Defaults below already match the
# team's shared instance - you only need to supply DB_PASSWORD.
DB_HOST=tp02fit5120.c1qymwwke45u.ap-southeast-2.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_NAME=onboarding
DB_PASSWORD=
# Ask the team for DB_PASSWORD - deliberately not committed to the repo.

# --- Auth / JWT ---
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-before-deploy
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Routing service (US 1.1 / 1.2) ---
# Swap for a real provider when integrated, e.g. OSRM self-hosted or
# GraphHopper / OpenRouteService free tier. Leave as "mock" to use the
# built-in fixture routes while the real service isn't wired up yet.
ROUTING_PROVIDER=mock
ROUTING_SERVICE_URL=http://localhost:5001
ROUTING_SERVICE_API_KEY=

# --- Congestion / sensory thresholds ---
# Multiplier over the DS2 baseline median that flags a corridor as HIGH.
# DS2 determines this empirically from real data - this is just the
# fallback default so the API works before that number is confirmed.
CROWD_HIGH_THRESHOLD_MULTIPLIER=1.5
MIN_BASELINE_OBSERVATIONS=5

# --- CORS ---
FRONTEND_ORIGIN=http://localhost:5173
